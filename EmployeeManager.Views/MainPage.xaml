<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:EmployeeManager.ViewModels;assembly=EmployeeManager.ViewModels"
             x:Class="EmployeeManager.Views.MainPage"
             BindingContext="{Binding Source={x:Static viewModels:DesignTimeViewModelLocator.MainPageViewModel}}"
             Title="Menu">
  <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
    <Button Text="Section List" Command="{Binding NavigationSectionListCommand}"/>
    <Button Text="DeepLink by Literal" Command="{Binding DeepLinkByLiteralCommand}"/>
    <Button Text="DeepLink by ViewModel" Command="{Binding DeepLinkByViewModelCommand}"/>
  </StackLayout>
</ContentPage>